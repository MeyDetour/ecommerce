
<a
      {% if route_name!= '' %}
        href="{{ path(route_name,param) }}"
        {% endif %}
        class="{{ aClass }}">

            <img src="{{ vich_uploader_asset(image)}}" class="{{ imgClass }}" alt="{{ image.imageName }}">

</a>

{#


TO CALL IT USE :

{% for image in bloop.images %}
    {% include 'image.html.twig' with  {
        'route_name':'delete_image',
        'imgClass':'imageAdded',
        'image':image,
        'param':{'id':image.id},
        'aClass':''} %}
{% endfor %}



}#}
