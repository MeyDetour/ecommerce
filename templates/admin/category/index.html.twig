{% extends './admin/base.html.twig' %}
{% block title %}Catégories{% endblock %}


{% block body %}

         <div class="d-flex flex-row gap-3 mb-5 pb-5 border-bottom ">
             <a class="btn-category"  href="{{ path('new_category') }}">new Category</a>

         </div>

    <table class="table">
        <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">name</th>
            <th scope="col">action</th>
        </tr>
        </thead>
         <tbody>
         {% for cat  in categories %}

         <tr>
            <th scope="row">{{ loop.index }}</th>
            <td><a href="{{ path('edit_category_admin',{'id':cat.id}) }}">{{ cat.name }}</a> </td>
            <td><a href="{{ path('del_category',{'id':cat.id}) }}">Delete</a></td>
        </tr>
        {% endfor %}

        </tbody>
    </table>
{% endblock %}
